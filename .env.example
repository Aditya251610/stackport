# Lives only on the VM as .env (never committed) — the deploy workflow
# writes DOMAIN and IMAGE here on every deploy; add app secrets here too
# once the app actually reads them (currently unused).

DOMAIN=dev.stackport.co.in
IMAGE=asia-south1-docker.pkg.dev/stackport-server-staging/stackport-staging/tunnel-server:latest
